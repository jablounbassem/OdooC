<table border="2">
    <tr>
        <td>Name</td>
        <td>Id</td>
    </tr>
    {% for partenr in partners %}

    <tr>
        <td>{{ partenr.name }}</td>
        <td>{{ partenr.id }}</td>
    </tr>
    {% endfor %}

</table>
